import sublime
import sublime_plugin

class amperimetro:
	def __init__(self, identif, periodo, espera):
		self.identif = identif
		self.periodo = periodo
		self.espera = espera
		self.tabla_amp = []

	def medir(amp)
		self.tabla_amp.append(amp)